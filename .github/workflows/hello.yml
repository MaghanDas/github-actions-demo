name: Hello Workflow

on: [push]  # Trigger this workflow every time code is pushed

jobs:
  hello-job:
    runs-on: ubuntu-latest  # Use a Linux machine

    steps:
      - name: Say Hello
        run: echo "👋 Hello from GitHub Actions!"
